"""Configuration files this plugin adds or overrides for PyPI publishing.

Mirrors pyrig's config-file discovery namespace, so configurations defined
here extend or override the base project's managed configuration.
"""
